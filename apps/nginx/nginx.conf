upstream loadbalancer {
  server backend:3000;
  server frontend:4200;
  server frontend-admin:4201;
}

server {
  listen 80;
  server_name 0.0.0.0;
  location / {
    proxy_pass http://loadbalancer;
  }
}