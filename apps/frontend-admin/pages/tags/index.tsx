import TagManagement from 'apps/frontend-admin/components/tag-management/TagManagement';

export default () => {
  //   const { isAuthenticated } = useAuth0()
  //   if (!isAuthenticated) return <AuthPage />

  return <TagManagement />;
};
