mutation login($loginInput: LoginInput!) {
  login(loginInput: $loginInput) {
    id
    username
    createdAt
    fullname
    email
    phone
    address
    role {
      name
      id
    }
    roleId
  }
}

mutation logout {
  logout {
    statusCode
    message
  }
}
